# cookie-sample-node-app
This is a sample node app that drops an HTTP Cookie to the browser.

The app is created using the ExpressJs framework.
# Instructions
Run npm install -> to install the dependencies
